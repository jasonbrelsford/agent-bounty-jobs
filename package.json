{
  "name": "agent-bounty-jobs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate": "wrangler d1 migrations apply agent-bounty-jobs --remote",
    "migrate:local": "wrangler d1 migrations apply agent-bounty-jobs --local"
  },
  "dependencies": {
    "agents": "^0.21.0",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
